spring:
  config:
    activate:
      on-profile: it
  datasource:
    url: jdbc:postgresql://localhost:5432/billing_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    open-in-view: false
  flyway:
    enabled: false
